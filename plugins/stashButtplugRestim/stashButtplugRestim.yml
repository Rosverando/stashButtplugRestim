name: stashButtplugRestim
description: Connects StashApp to Buttplug.io/Intiface
version: 1.0
ui:
  javascript:
    - stashButtplugRestim.js
  csp:
    script-src:
      - https://cdn.jsdelivr.net
    connect-src:
      - ws://localhost:12346
      - ws://127.0.0.1:12346
      - ws://localhost:12345
      - ws://127.0.0.1:12345
      - ws://localhost
      - ws://127.0.0.1
      - "*"

settings:
  skipButtplug:
    type: BOOLEAN
    displayName: Direct Tcode
    description: Wether you want to skip Buttplug and send straight Tcode to Server URL
  serverUrl:
    type: STRING
    displayName: Server URL
    description: The address of your websocket server. Most Likely Intiface Central (e.g. ws://127.0.0.1:12345). In case you are using Restim and want to skip buttplug the url should be something like ws://localhost:12346/tcode)
  latency:
    type: NUMBER
    displayName: Latency (ms)
    description: Adjust timing synchronization (negative to have actions play earlier)
  invert:
    type: BOOLEAN
    displayName: Invert Position
    description: Inverts the position, 0 is now 100 and 100 is now 0
  cumHotkey:
    type: BOOLEAN
    displayName: Cum Hotkey
    description: When pressing PAUSE on the keyboard, script playback will be stopped and a final command to got to <Cum Hotkey Position> in 0ms will be sent. To resume script playback, press PAUSE again.
  cumHotkeyPosition:
    type: NUMBER
    displayName:  Cum Hotkey Position
    description: The Position (0-100) the Device will go to when pressing the Cum Hotkey
  bugfix:
    type: BOOLEAN
    displayName: bugfix
    description: Bugfix for Buttplugs wrong tcode max, limits stroke to 0-99, will be removed once Buttplug fixes it on their end
